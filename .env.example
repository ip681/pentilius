# Copy to .env at the repo root — docker-compose reads this automatically
# for the Postgres container. apps/api and apps/web have their own
# .env.example files with the variables each app actually needs.
POSTGRES_USER=pentilius
POSTGRES_PASSWORD=pentilius
POSTGRES_DB=pentilius
